import type { Invoice } from '@/types/payments'

export async function fetchInvoices(): Promise<Invoice[]> {
  // TODO: wire to backend REST endpoint when available (currently HTML-based)
  return []
}
